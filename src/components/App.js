import React from 'react';

import Board from '../containers/Board';
import '../assets/stylesheets/app.css';

const app = props => {
    return (
        <div className='App'>
            <Board />
        </div>
    );
};

export default app;
