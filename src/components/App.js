import React from 'react';

import Board from '../containers/Board';

const app = props => {
    return (
        <div className='App'>
            <Board />
        </div>
    );
};

export default app;
